export default function AdminComingSoon({ section }) {
  return (
    <div className="admin-coming-soon">
      <h2>{section}</h2>
      <p>This section hasn't been built yet — tell Claude when you're ready to set it up.</p>
    </div>
  );
}